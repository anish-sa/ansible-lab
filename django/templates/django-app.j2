[Unit]
Description=Django App Service

[Service]
After=network.target
Type=simple
Restart=always
User=root
ExecStart={{ ansible_env.HOME }}/.pyenv/versions/{{ venv_name }}/bin/python {{ app_dir }}/manage.py runserver 0:8001

[Install]
WantedBy=multi-user.target
